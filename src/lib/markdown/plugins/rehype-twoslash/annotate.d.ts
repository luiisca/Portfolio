/**
 * @param {Root} tree
 *   Tree.
 * @param {TwoslashNode} annotation
 *   Annotation.
 * @param {State} state
 *   Info pased around.
 * @returns {Array<ElementContent>}
 *   Extra content for footer.
 */
export function annotate(tree: Root, annotation: TwoslashNode, state: State): Array<ElementContent>;
import type { Root } from 'hast';
import type { TwoslashNode } from 'twoslash';
import type { State } from './types.js';
import type { ElementContent } from 'hast';
//# sourceMappingURL=annotate.d.ts.map